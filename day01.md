# Day 01 — 피지컬 AI 입문

> 팀 모노리스 · 피지컬 AI 입문 과정 (강사: 김호성)
> 1일차: 피지컬 AI 개요 / 깃허브 포트폴리오 구축 / 티처블 머신 음성 인식

---

## 1교시 — 피지컬 AI란?

### 지금 어디까지 왔나
- 택배 분류 로봇은 비닐/박스 구분은 잘하지만, 상자가 엉키거나 많이 쌓이면 오류가 나고 초기화가 필요하다.
- 8시간짜리 작업을 로봇이 100시간 넘게 알아서 반복 → **지속성**이 로봇의 큰 장점 (상자 분류 약 3초, 자동 교대 근무).
- 인간 vs 로봇 택배 처리 대결: 간발의 차로 사람이 이겼지만, 쉬지 않고 일한다는 관점에서는 로봇이 유리.

### 피지컬 AI = 하드웨어 + AI의 두뇌
물리 세계에 적용되는 AI. **하드웨어에 AI가 붙으면 피지컬 AI**라고 본다.

관련 용어 정리:

| 용어 | 의미 |
|---|---|
| 온디바이스 AI | 인터넷 연결 없이 기기 자체에서 AI 처리 |
| 엣지 AI | 중앙 서버 없이 현장(엣지)에서 자체 처리 |
| 임베디드 AI | 소형 컨트롤러에 내장된 AI |

### 동작 원리: 인지 → 판단 → 행동
사람이 **보고 → 생각하고 → 움직이는** 과정을 기계가 그대로 따라 한다.

### 확장 분야 4가지
- 자율주행 / 스마트 모빌리티
- 스마트 제조 / 물류 로보틱스
- 스마트 홈 / 소셜 서비스
- 재난 구호 / 특수 산업 현장

### 두 갈래(하드웨어 · 소프트웨어)의 역사와 융합

**하드웨어 — 인간의 근력을 대체**
- 산업 혁명, 대량 생산과 컨베이어 벨트.
- GM의 **유니메이트(Unimate)**: 유압으로 뜨겁고 무거운 금속 부품을 옮김. 하지만 눈과 뇌가 없는, 좌표 기반의 자동화 기계에 가까웠다.

**소프트웨어 — 인간의 두뇌를 대체**
- 기계식 계산기 → 물리적 움직임으로 정보 처리.
- 최초의 전자식 컴퓨터 **에니악(ENIAC)**: 포탄 좌표 계산용.
- **1956년 다트머스 회의** — AI의 탄생. 단, 이때 AI는 로봇이 아니라 순수한 소프트웨어였다. 머리는 돌아갔지만 세상에 물리적 영향력을 줄 수 없었다.

**두 축의 융합 (5단계)**

| 단계 | 사례 | 특징 |
|---|---|---|
| 1. 셰이키(Shakey) | 모바일 로봇 | 바퀴 이동 + 카메라 인식 + 컴퓨터 계산. 룰(rule) 기반, 느렸지만 피지컬 AI 개념 확립 |
| 2. 산업용 로봇 팔 대중화 | 공장 조립 라인 | 스스로 판단 X, 사람이 준 좌표를 **정밀 제어**. SW 발전이 아닌 정밀 제어 |
| 3. 두 발로 걷기 | 혼다 아시모 | 고도로 프로그래밍됨. 정해진 조건에서 정해진 동작만. 미묘한 차이에 넘어졌지만 엄청난 시도 |
| 4. 스스로 배우기 | 딥러닝 · 강화학습 (2010s), Spot | 사람이 특징을 알려주던 시대 → 기계가 스스로 특징을 찾는 시대. 시뮬레이션 반복으로 최적 움직임 탐색 |
| 5. 말을 알아듣고 계획 | VLA 모델 | LLM을 로봇의 두뇌로. **시각-언어-행동(Vision-Language-Action)**을 하나로 연결, 맥락 이해 후 스스로 계획 |

### 피지컬 AI의 다음 — AGI의 물리적 구현

1. **범용 휴머노이드의 대중화** — 단일 역할 → 멀티태스킹 일반화. 인간과 같은 환경에서 작동하려니 휴머노이드 형태가 된다. (26/8/3 기준, 구글 로봇이 얇은 비닐을 묶고 지퍼백을 닫는 세밀한 작업 수행 중)
2. **Sim2Real (가상에서 배우고 현실로)** — 현실에서 직접 학습하면 부수고 시간도 오래 걸림. 가상 공간(예: NVIDIA Isaac Sim)에서 무한히 연습한 뒤 현실로 옮긴다.
3. **클라우드 공유학습 (한 대가 배우면 전부가 안다)** — 서울 로봇이 새 문손잡이 여는 법을 터득하면 뉴욕 로봇이 즉시 그 문을 연다. (예: 테슬라가 전국 차량 데이터를 수집)

### 우리는 무엇을 준비할까
> "신체로 반복하는 일" → "시스템을 운영하는 일"로

1. **실행자에서 감독자로** — 로봇은 반드시 멈춘다. 현장 경험자가 로봇 티처(내 노하우를 학습 데이터로)/트러블 슈터(예외 해결) 역할. **도메인 지식**이 매우 중요.
2. **기계가 못하는 손의 일** — 비정형·섬세한 손일(수공예, 식재료 손질, 배관 수리), 물리적 교감(요양, 물리치료, PT)은 당분간 인간의 일로 남는다.
3. **지금부터 할 수 있는 것** — 도구로 보기 / 내 일 쪼개보기 / 기초 리터러시 / 직무 재교육 참여. 융합 영역: 웹·코딩, 회로·센서, AI·학습, 통신·제어, 조립·제작.

**핵심 마인드셋: "무엇이든 배워서 만들 수 있다"**

---

## 2교시 — 깃허브로 포트폴리오 페이지 구축

### 왜 깃허브인가
- 클라우드에 내 코드를 공유하는 곳. 이제는 **프로젝트를 보여주는 이력서** 역할.
- 실수로 코드가 지워지는 것 방지, 공동작업 시 충돌 방지, 변경 내용 체계적 관리.
- **Star 시스템**으로 프로젝트 평판이 쌓인다.

### 실습 절차 (GUI 방식)
1. 배포된 소스 코드(`online_day1_coding`)를 받아 `index.html`을 메모장에서 편집 (`Ctrl+F`로 바꿀 문구 찾기).
2. GitHub에서 **New → Create repository**로 저장소 생성 = 가상 클라우드에 내 코드 저장소를 만든 것.
3. 아무것도 건드리지 말고 **Quick setup → uploading an existing file**로 파일 업로드.
4. **Commit changes** → 첫 업로드 완료.
5. **Settings → Pages** 이동 → Branch를 `None`에서 `main`으로 바꾸고 **Save**.
6. 잠시 기다리면 `Your site is live at ...` URL이 배포됨. **Visit site**로 확인.

### 로컬 vs 서버
| | 의미 |
|---|---|
| 로컬(local) | 내 컴퓨터 안에서만 접근 가능 |
| 서버(server) | 인터넷에서 누구나 접근 가능한 상태 |
> 깃허브 페이지에 올린 순간, 로컬 → 서버로 나온 것.

### 이미지/스타일 바꿔보기
- 이미지: `index.html`에서 이미지 주소를 `Ctrl+F`로 찾아 `robot.jpg`로 변경 → Add file로 이미지 업로드(commit) → 기존 `index.html`도 최신 버전으로 교체.
- 스타일: `style.css`를 수정해 웹페이지 스타일을 간단히 변경. (hex 코드로 색상 확인 가능)
> 원래는 git 명령어(CLI)로 하지만, 오늘은 GUI로 진행.

---

## 3교시 — 티처블 머신으로 음성 인식 AI

### AI = 학습 + 추론 + 지각

**컴퓨터 학습의 3가지**
| 방식 | 설명 |
|---|---|
| 지도 학습 | 정답(라벨)을 붙여 가르친다 |
| 비지도 학습 | 정답 없이 비슷한 것끼리 묶는다 |
| 강화 학습 | 환경을 주고 보상/벌로 스스로 익히게 한다 |

오늘은 **지도 학습** — 정답을 붙여 가르친다. 원래 CV(이미지)에서 라벨링하듯, 오늘은 사진 대신 **소리**로.

**다루는 데이터에 따른 분야**: CV / 음성·오디오 처리 / 자연어 처리 → VLA 모델은 이들을 합친 것.

### 소리는 어떻게 데이터가 되는가 (핵심 원리)

소리를 AI가 학습하려면, 눈에 보이는 2D 이미지로 바꿔야 한다. 그 과정이 **파형 → 짧게 자르기 → 스펙트로그램** 3단계다.

**1단계. 파형(Waveform) — 시간에 따른 공기 진동의 세기**
- 파형은 "언제 소리가 컸다/작았다(진폭)"는 정보는 담지만, **"그 소리가 어떤 음높이(주파수)로 이루어졌는가"는 안 보인다.**
- 예: 피아노로 도(261Hz)를 치든 미(330Hz)를 치든, 파형만 보면 둘 다 그냥 출렁이는 그래프라 눈으로 주파수를 구분하기 어렵다.
- → 그래서 파형을 **주파수가 보이는 형태**로 바꾸고 싶다. 그 도구가 **푸리에 변환(Fourier Transform)**.

**2단계. 짧게 자르기 — STFT (Short-Time Fourier Transform)**
- 파형 전체를 통째로 푸리에 변환하면 문제가 생긴다: **"몇 초에 어떤 주파수가 났는지"라는 시간 정보가 통째로 사라진다.** 전체에 어떤 주파수가 섞였는지만 알 뿐, 그게 1초 지점인지 10초 지점인지 알 수 없다.
- 그래서 전체를 아주 짧은 구간(예: 25ms)으로 잘게 쪼갠다. 짧은 구간 안에서는 신호가 거의 일정하다고 가정할 수 있다.
- 각 조각마다 따로 푸리에 변환 → **시간 축은 유지하면서 각 시점의 주파수 정보**를 얻는다.
- (실전 디테일: 조각을 겹쳐서 자르고, 경계에는 윈도우 함수(Hann 등)를 곱해 경계에서 생기는 인공 잡음(spectral leakage)을 줄인다.)

**3단계. 스펙트로그램(Spectrogram) — 2D 이미지**
- 각 짧은 조각의 "주파수별 세기"를 시간 순서대로 옆으로 쌓으면 2D 이미지가 된다.
- **가로축 = 시간 / 세로축 = 주파수 / 색(밝기) = 그 시간·그 주파수에서의 세기**
- 노란색일수록 그 순간 그 주파수 성분이 강하다는 뜻. 이렇게 소리가 "이미지"가 되어, CV처럼 AI가 학습할 수 있게 된다.

> 요약: **파형(시간+세기) → STFT(짧게 잘라 시간별 주파수) → 스펙트로그램(시간·주파수·세기의 2D 이미지)**

### 티처블 머신 실습
- https://teachablemachine.withgoogle.com/ → 시작하기 → **오디오 프로젝트**.
- "배경소음" 클래스를 먼저 학습시키는 이유: 환경마다 소음이 다르므로, 지금 환경을 default로 잡아준다.
- class2에 "켜기 / 끄기"를 넣고 데이터 학습.
  - 20초 정도 녹음 → 샘플 약 20개 추출 (최소 8개 필요).
- **중요: 이 모델에 "모른다"는 없다.** 단어의 뜻을 모르고, 비슷한 소리일수록 높은 확률로 맞춘다. 없는 단어여도 확률로 어느 한 클래스에 분류된다. **확률 기반 분류.**

**잘 안 될 때 — 양질의 데이터 학습법**
- 데이터 다양성 증가: 마이크와의 거리 조절, 높낮이·억양 변형.
- 소음 배제 관리: 녹음 시 기침·타이핑 소리 빼기.
- 재학습: 보완된 데이터로 모델 다시 빌드.

**배포**
- 모델 내보내기 → TensorFlow.js 등으로 export → 공유 가능한 링크 생성 → 웹 포트폴리오에 적용 가능.

---

*이 문서는 수업 필기를 정리하고, 소리 파형 원리 부분을 추가로 학습해 다시 정리한 것입니다.*
